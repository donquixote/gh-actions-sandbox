name: test
on:
  push:
    branches:
      - '*'

jobs:
  test-gh-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Test gh release list 1
        run: |
          gh release list
      - name: Test gh release list 2
        run: |
          gh release list --search 1.0.0
      - name: Test gh release list 3
        run: |
          gh release list --search 1.0.0
          gh release list --search 1.0.0 --json name
          gh release list --search 1.0.0 --json name --jq '.[].name'
          gh release list --search 1.0.0 --json tagName
          gh release list
          gh release list --exclude-drafts
      - name: Test gh release view
        run: |
          gh release view 1.0.0
